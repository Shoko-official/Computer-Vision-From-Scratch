# Temporary, until pyproject.toml exists. Nothing here is a runtime dependency:
# src/cvfs/ imports the standard library only, and there's a test that enforces
# that by parsing the package rather than by trusting this file.

pytest
ruff

# Only for drawing figures for docs/, never imported from src/cvfs/. It drags in
# numpy and pillow, which is exactly why the ban is checked with ast instead of
# by looking at what's installed.
matplotlib
